package com.dagger.taxiradar.vehicleradar

import javax.inject.Inject

// TODO The Api will be provide by a third module as bridges so this example will not show the retrofit for now
class VehicleRadarRepositoryImpl @Inject constructor(
//    private val vehicleRadarApi: VehicleRadarApi
) : VehicleRadarRepository
